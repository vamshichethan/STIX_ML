FROM python:3.11-slim

WORKDIR /app

# Install system dependencies (build-essential for compiling neo4j drivers/ml models, etc)
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    libxml2-dev \
    libxslt1-dev \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# PyTorch geometric dependencies can be tricky, typically better handled separately, but we will rely on standard torch for this prototype.

COPY . .

CMD ["sh", "-c", "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"]
